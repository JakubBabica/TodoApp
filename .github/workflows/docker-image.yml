name: CI/CD

on: push

jobs:
  build:
    name: Building image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Buildx
        id: buildx
        uses: docker/setup-buildx-action@v1

      - name: DockerHub login
        uses: docker/login-action@v1
        with:
          username: ${{ GIT-PAT }}
          password: ${{ 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 }}

      - name: Build and push
        uses: docker/build-push-action@v2
        with:
          push: true
          tags: babicajakub/mywebapp:latest  # Please change the username here